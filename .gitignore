# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Local config and secrets
.env

# Downloaded embedding model cache (mounted into the embedder)
models/

# Postgres data (when bind-mounted)
pgdata/
